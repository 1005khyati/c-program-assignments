/*WAP to input the radius of circle and print the area of circle*/
#include<stdio.h>
float main()
{
	float pi=3.14,r=2,radius,area;
	printf("Radius of circle : %f",r);
	radius=r*r;
	area=pi*radius;
	printf("\nArea of circle : %f",area);
	return 0;
}
